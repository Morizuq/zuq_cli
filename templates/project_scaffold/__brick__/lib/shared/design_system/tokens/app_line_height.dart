/// Spacing tokens representing line heights (multipliers) to ensure consistent legibility.
class AppLineHeight {
  AppLineHeight._();

  /// Default line height for body text (1.5).
  static const double body = 1.5;

  /// Default line height for headings and dense labels (1.2).
  static const double heading = 1.25;
}
