/// Border width design tokens to maintain visual hierarchy.
class AppBorderWidth {
  AppBorderWidth._();

  /// Extremely thin borders, typically used for subtle divider lines or container borders.
  static const double hairline = 0.5;

  /// Thicker borders, typically used to emphasize active states, accent borders, or indicators.
  static const double accent = 2.0;
}
