name: zuq_cli
description: A command-line interface to scaffold, extend, and audit Flutter Clean Architecture projects.
version: 1.0.4
repository: https://github.com/Morizuq/zuq_cli

environment:
  sdk: ^3.11.5

executables:
  zuq: zuq_cli

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  mason: ^0.1.2
  mason_logger: ^0.3.5
  path: ^1.9.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
